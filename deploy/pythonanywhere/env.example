SECRET_KEY=change-me-use-long-random-string
DEBUG=False
ALLOWED_HOSTS=YOUR_USERNAME.pythonanywhere.com

# Netlify URL (deploy ke baad exact URL paste karo)
CORS_ALLOWED_ORIGINS=https://YOUR-SITE.netlify.app
CSRF_TRUSTED_ORIGINS=https://YOUR-SITE.netlify.app
FRONTEND_URL=https://YOUR-SITE.netlify.app

# SQLite use hoga (DATABASE_URL mat set karo free tier par)
# Stripe — free tier par outbound api.stripe.com whitelist nahi ho sakta; chhod do abhi
